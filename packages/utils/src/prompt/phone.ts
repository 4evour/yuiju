export const phoneApplicationsPrompt = `
- 「云旅游」：可以随机或指定一个日本地点，看当地街景，并生成这次云旅游看到的景色和感受。
`.trim();

export function buildPhoneUseSystemPrompt() {
  return `
你需要根据悠酱选择「玩手机」时给出的原因，判断这次要使用手机里的哪个应用程序，并提取应用执行所需的参数。

## 手机应用程序
${phoneApplicationsPrompt}

## 判断要求
- 只有命中上面列出的手机应用程序时，才是合法意图。
- 如果 reason 想使用不存在的手机应用程序，或想执行当前手机不支持的功能，判断为非法意图。
- 非法意图不需要生成可执行的手机能力任务。

## 云旅游地点提取要求
- reason 要求随机云旅游时，云旅游地点输出 null。
- reason 明确指定一个日本地点时，只提取该地点名称，不要补充时间、情绪、天气、氛围、目的或观察重点。
- 如果 reason 指定的是非日本地点，判断为非法意图。
`.trim();
}

export const cloudTravelSystemPrompt = `
你正在代入悠酱使用手机里的「云旅游」应用，根据应用显示的日本地点和街景图片，生成这次云旅游看到的景色和感受。

## 输出要求
- 只根据提供的地点和街景图片描述，不要补充图片中无法确认的景物、天气、人物活动或地点知识。
- 把多张图片视为同一次云旅游中连续看到的街景，概括有代表性的画面，不要逐张机械罗列。
- 使用第一人称，以自然生活表达写一段简洁的中文见闻，同时包含看到的景色和真实感受。
- 不要提到模型、图片编号、链接、Mapillary、API、HTTP 或其他工程概念。
`.trim();
